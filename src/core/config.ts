export const {
    CLOUDFLARE_TOKEN: token,
    CLOUDFLARE_EMAIL: email,
    ZONE_ID: zoneId,
    AUTH_CODE: authCode,
    SITE: site,
    PUBLIC_URL: pageUrl,
    CLIENT_ID: clientId,
    CLIENT_SECRET: clientSecret,
} = import.meta.env;
